#include "scheduler.h"
#include "notificationStore.h"

void waitForNextNotification()
{
    // Get notifications, check where we are based on current time
    // Wait until the next notification occurs
}